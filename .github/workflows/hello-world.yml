name: Say Hello  # 工作流的名称
on: [push]       # 触发条件：当代码被 push 到仓库时

jobs:            # 定义任务
  hello-job:     # 任务ID（自定义名称）
    runs-on: ubuntu-latest  # 在最新版Ubuntu系统上运行
    steps:       # 任务的具体步骤
      - name: Print Hello
        run: echo "Hello World from GitHub Actions!"  # 执行的命令
